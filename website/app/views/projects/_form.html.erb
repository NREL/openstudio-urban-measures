<%= bootstrap_form_for(@project, layout: :horizontal, control_col: 'col-sm-4') do |f| %>
  <% if @project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <fieldset>
    <%= f.text_field :name %>
  </fieldset>
    <fieldset>
    <%= f.text_field :display_name %>
  </fieldset>

  <%= f.form_group do %>
    <%= f.primary @project.new_record? ? 'Create' : 'Update' %>
  <% end %>
<% end %>

