<h2>Workflow</h2>

<h3 class="edit-name"><%= @workflow.id %> <%= link_to "[ edit ]", edit_workflow_path(@workflow), class: "edit-link" %></h3>

<table class="table table-striped">
  <% @workflow.attributes.keys.sort.each do |key| %>
    <tr>
      <th> <%= key == "_id" ? "ID" : key.humanize%></th>
      <td><%= @workflow[key] %></td>
    </tr>
   <% end %>
</table>

