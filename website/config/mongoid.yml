development:
  # Configure available database sessions. (required)
  sessions:
    # Defines the default session. (required)
    default:
      # Defines the name of the default database that Mongoid can connect to.
      # (required).
      database: openstudio_urban_modeling_dev
      # Provides the hosts the default session can connect to. Must be an array
      # of host:port pairs. (required)
      hosts:
        #- 0.0.0.0:27017
        - 127.0.0.1:27017
        #- localhost:27017
      options:
  options:
production:
  # Configure available database sessions. (required)
  sessions:
    # Defines the default session. (required)
    default:
      # Defines the name of the default database that Mongoid can connect to.
      # (required).
      database: openstudio_urban_modeling_prod
      # Provides the hosts the default session can connect to. Must be an array
      # of host:port pairs. (required)
      hosts:
        - 10.1.1.93:27017
        #- localhost:27017
      options:
        pool_size: 16
  options:
docker:
  # Configure available database sessions. (required)
  sessions:
    # Defines the default session. (required)
    default:
      # Defines the name of the default database that Mongoid can connect to.
      # (required).
      database: openstudio_urban_modeling_prod
      # Provides the hosts the default session can connect to. Must be an array
      # of host:port pairs. (required)
      hosts:
        - insight4.hpc.nrel.gov:27018
        #- localhost:27017
      options:
        pool_size: 16
  options:
docker-dev:
  # Configure available database sessions. (required)
  sessions:
    # Defines the default session. (required)
    default:
      # Defines the name of the default database that Mongoid can connect to.
      # (required).
      database: openstudio_urban_modeling_prod
      # Provides the hosts the default session can connect to. Must be an array
      # of host:port pairs. (required)
      hosts:
        - insight4.hpc.nrel.gov:27018
        #- localhost:27017
      options:
        pool_size: 16
  options:
test:
  sessions:
    default:
      database: openstudio_urban_modeling_test
      hosts:
        - localhost:27017
      options:
        read: primary
        # In the test environment we lower the retries and retry interval to
        # low amounts for fast failures.
        max_retries: 1
        retry_interval: 0